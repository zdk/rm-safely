FROM ubuntu:24.04

RUN apt-get update && apt-get install -y --no-install-recommends \
    bash zsh coreutils \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY rm-safely ./
COPY tests/ ./tests/

RUN bash rm-safely install

CMD ["bash", "tests/test_e2e.sh"]
